# Server Configuration
PORT=8070
NODE_ENV=development

# Database Configuration
DB_DIR=data
DB_PATH=data/job_tracker.db

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000  # 15 minutes in milliseconds
RATE_LIMIT_MAX_REQUESTS=100  # Maximum requests per IP in the time window

# Other Settings
DEBUG_DB=true  # Set to false in production
