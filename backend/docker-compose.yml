version: '3'

services:
  job-tracker-api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: job-tracker-api
    ports:
      - "8070:8070"
    volumes:
      - job-tracker-data:/app/data
    restart: unless-stopped
    environment:
      - NODE_ENV=production
      - PORT=8070

volumes:
  job-tracker-data:
